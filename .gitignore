# Godot 4+ specific ignores
.godot/
.import/

# (export_presets.cfg는 일부러 커밋한다 — 배포 설정 재현용)

# Exported / build artifacts
*.pck
*.zip
*.exe
*.dmg
*.apk
*.aab
build/
exports/

# Node (검증 도구 touch_verify.js 가 쓰는 playwright — 로컬에만 둔다)
node_modules/
package.json
package-lock.json

# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
