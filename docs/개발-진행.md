# minho2 개발 진행 (핸드오프)

> 새 세션은 이 문서부터 읽고 *이어간다*. 매번 처음부터 다시 묻지 않는다.

## 프로젝트 한 줄
형(신기훈)이 아들 민호와 만드는 **두 번째 게임**. 첫 게임 mino1을 졸업하고 **Godot 엔진으로 한 차원 높여** 새로 시작한다. 목적은 "민호랑 즐겁게"이되, **민호 눈높이가 높아져서 '진짜 게임처럼' 보여야 한다.**

## 결정된 것 (2026-06-20)
- **엔진 = Godot 4.** 이유: 움직이는 그림·물리·파티클·소리가 기본 기능 → "장난감 티" 탈출. 웹(HTML5) export 가능 → 민호는 링크로 폰 플레이 유지. (mino1의 Phaser 웹 2D는 에셋을 손으로 다 깎아야 해서 70점 천장이었음.)
- **목적 = 민호와 즐겁게 (포폴·수입 1순위 아님).** 단 민호 눈높이 높음 = 핵심 조건.
- **방향 3원칙** (mino1 교훈에서 도출): ①재미 한 가지를 먼저 한 문장으로 박기 ②에셋(움직이는 그림·소리)을 1순위로, AI로 일관되게 뽑기 ③적은 에셋으로 깊게 노는 장르를 영리하게 고르기.
- **"수준"의 정체** = 3D가 아니라 애니메이션 + 사운드 + 폴리시(매끈함). (안내판 CLAUDE.md에 상세.)

## 아직 안 정한 것 (병목 — 형 결정 필요)
1. **장르·핵심 재미** — "이 게임은 30초에 한 번 ___하는 맛" 한 문장. (형 미감·민호 취향 필요.)
2. **민호가 평소 자주 하는 게임** — 민호 눈높이의 실제 기준. 형에게 물어볼 것.

## 환경·배포 (2026-06-20 구축)
- **Godot 4.3** = `~/godot/Godot_v4.3-stable_linux.x86_64` (WSL, 헤드리스로 import·export 가능). export 템플릿도 설치됨.
- **GitHub repo** = `joyhunny/minho2` (public). 소스=`master`, 웹빌드=`gh-pages`.
- **민호 폰 접속 주소** = https://joyhunny.github.io/minho2/
- **재배포 = `bash tools/deploy.sh`** (import→web export→gh-pages push 한 방). single-thread export라 Pages에서 헤더 설정 없이 바로 열림.
- **한글 폰트** = `fonts/Jua-Regular.ttf` (둥근 한글). 라벨에 `_make_label()`로 적용.

## 다음 할 일 (우선순위)
1. **★형/민호 실기기 확인** — https://joyhunny.github.io/minho2/ 를 형 PC·민호 폰 브라우저에서 열어 화면·한글·**터치 조작(이동·공격)**이 되는지. (헤드리스 환경이라 *화면*은 실기기로만 최종 확인.)
   - **2026-06-21: 모바일 이동·공격 안 되던 버그 수정 완료** → 재배포함. 형/민호 폰에서 ① 왼쪽 드래그로 이동 ② 우하단 칼 버튼 공격 ③ 우상단 STAT·가방 버튼 열고 닫은 뒤 다시 이동되는지 확인 부탁. (원인·검증은 아래 작업 로그 참고.)
2. 장르·핵심 재미 형과 합의 (후보 추천 → 형 결정). ※ 지금 별받기는 'Godot 워크플로우 시범'일 뿐, 본 게임 장르는 미정.
3. 민호 평소 게임 청취 → 아트 톤·첫 목표 조정.
4. **에셋 일관성 테스트(착수 전 게이트):** AI로 "같은 캐릭터 여러 동작 그림"이 일관되게 나오나 먼저 확인. (도트 스타일 일관성 유리한지 등.)

## 작업 로그
- 2026-06-21: **★모바일 이동·공격 안 되던 블로커 버그 수정.** 증상: 폰에서 조이스틱 이동도, 칼(공격) 버튼도 안 됨. **원인 = 스탯창(`ui/StatPanel.gd`)·가방창(`ui/InventoryPanel.gd`)이 *닫혀 있을 때도* 화면 전체를 덮은 채 `MOUSE_FILTER_STOP` 이라 모든 터치를 먹어버림** (이 두 컨트롤은 우상단 토글버튼[STAT·가방]을 그리려고 전체화면인데, STOP이면 빈 곳 터치까지 GUI가 소비 → `_unhandled_input`[조이스틱]·공격버튼[하위 레이어]에 입력이 안 내려감). 작성자가 "안 누르면 통과"로 착각했으나 STOP은 영역 전체를 소비함. **수정 = 잘 작동하던 패턴(skill_tray·cook_hud = IGNORE + Main 이 먼저 hit-test)에 맞춤:** 두 패널을 *닫히면 `MOUSE_FILTER_IGNORE`(통과)·열리면 `STOP`(모달)* 로 토글하고, 닫힘 상태의 토글버튼은 `try_button(pos)` 를 `Main._handle_touch` 가 먼저 검사(조이스틱/공격 앞). **검증: 새 헤드리스 입력 테스트 `tools/input_test.gd` — 실제 InputEvent 를 캔버스 좌표(720×1280, 실기기와 동일 좌표계)로 주입해 15/15 ALL PASS**(블로커 없음·터치 조이스틱 활성·플레이어 80px 이동·공격버튼 터치/마우스·STAT/가방 열고닫기·닫은 뒤 이동 복귀). S6 회귀 ALL PASS. 웹 재빌드·배포. *(헤드리스 창이 작아 push_input 의 창→캔버스 stretch 가 좌표를 20배 키우는 artifact 가 있어, `in_local_coords=true` 로 그 변환을 건너뛰고 실기기와 같은 좌표로 검증함. 실브라우저 Playwright 검증은 이 WSL 에 `libnspr4.so` 부재로 막혀 못 돌림 → 실기기 최종 확인은 형/민호 폰 몫.)* — *이유: 형 보고 "모바일에서 이동·칼 안 됨". 한 번도 실화면 입력을 검증 못 한 채(전부 함수호출 테스트) 배포돼 처음부터 막혀 있었음 → 이번에 실좌표 입력 경로를 테스트로 박제해 복리 방지.*
- 2026-06-20: 프로젝트 셋업 — 폴더·git 저장소·안내판(CLAUDE.md)·.gitignore·이 진행 문서 생성. brain에 `/minho2` 스킬 추가. 엔진(Godot)·목적·3원칙·방향 합의. — *이유: mino1이 에셋 천장에 막혀, 에셋·폴리시가 기본인 엔진에서 민호 높아진 눈높이를 맞추기 위해.*
- 2026-06-20: Godot 4.3 설치 + 첫 시범 게임 "떨어지는 별 받기"(`main.gd`) 제작 → 한글화(Jua 폰트) → 검증(스크립트 파싱 0에러·런타임 180프레임 0에러·web export 성공) → GitHub Pages 배포(https://joyhunny.github.io/minho2/). `tools/deploy.sh`로 재배포 자동화. — *이유: 형 지시 "Godot로 만들어 민호 폰에 배포, 검증까지" → 시범 한 판으로 제작→검증→폰배포 전체 워크플로우를 깔아둠. (시각 화면은 WSL에 브라우저가 없어 실기기 확인 필요.)*
- 2026-06-20: **mino1 전체 포팅 결정 + 설계도(`docs/포팅-설계도.md`) 작성** — mino1을 Godot로 옮기되 "옮기며 업그레이드"(애니·소리·이펙트·물리). 슬라이스 S0~S6로 분할. — *이유: 시스템(8천 줄)을 한 번에 못 옮기니 굴러가는 조각씩 쌓는다.*
- 2026-06-20: **★S0 — 기반 뼈대 완료.** ①mino1 캐릭터·적 스프라이트 12장 → `assets/sprites/`(styled 버전만, hero/slime/rat/pig/crow/toad/hedgehog/boar/croc/wolf/capybara/boss_elephant). ②`autoload/GameData.gd` — core.js 데이터 *전부* 1:1 이식(ENEMY 9·REGION 4·SKILL 8·RARITY 5·ITEM **48**·DIFFICULTY 10·GOLD표) + 공식(makeRNG·clamp·pickEnemyType·rollRarity·tryDrop). 밸런스 숫자 그대로 보존. ③`autoload/GameState.gd` — freshPlayer() 1:1 + user://save.json 저장/복원(라운드트립 검증 통과). ④`scenes/Main.tscn/gd` — 2200² 월드·코드생성 풀밭 타일(mino1 grass_soft 동일 레이어)·플레이어 추적 Camera2D(경계 제한). ⑤`scenes/Player.tscn/gd`(CharacterBody2D) — 이동(가상 조이스틱 왼쪽60%·max62px + WASD/화살표)·바라보는 방향·발밑 그림자·걷기 의사애니(bob). ⑥별받기는 `prototypes/`로 이동, main_scene을 `scenes/Main.tscn`으로 교체. **검증: import 0에러 · 런타임 0에러 · save/load 라운드트립 OK · 데이터 카운트 전부 일치.** — *이유: 설계도 S0 "빈 맵에서 주인공이 움직인다". 데이터·상태·맵·이동의 토대를 깔아 S1(전투)부터 이 위에 쌓는다.*
- 2026-06-20: **★S6 — 연출·사운드 마감 완료 (포팅 끝).** ①**사운드 엔진** `autoload/Audio.gd`(오토로드 싱글톤, mino1 `sound.js` 1:1 이식) — 외부 음원 파일 0개로 GDScript 가 짧은 PCM 파형을 합성해 `AudioStreamWAV` 로 굽는다(다운로드·라이선스·인터넷 의존 0; CC0 다운로드는 시도했으나 소스가 불안정해 합성 채택 — 프롬프트의 명시 폴백). 효과음 12종(attack·hit·crit·kill·levelup·heal·hurt·roar·boom·tame·win·ui) + 합성 BGM 루프(16초, loop_forward) + 음소거 토글(GameState.muted 저장). 호출부 연결: 타격/치명타(`_update_combat` 잡몹·보스)·처치(`on_enemy_died`)·레벨업(`_gain_xp`·이스터에그)·피격(`on_player_hurt`·`hazard_damage`)·보스 포효(`Boss._ready` 인트로)·운석 폭발(`Boss._update_meteors`)·슬라임 소환(`Skills._cast_summon_ally`)·카피바라 힐(`Companion`)·승리(`on_boss_died`)·UI 탭(난이도/지도/레벨업 카드 선택). ②**인트로** `_show_intro`(어두운 오버레이+스토리 4줄 페이드인+mino 시그니처+"탭하여 시작", 5.5초/탭 종료) → 새 게임이면 난이도 선택창으로. ③**챕터 스토리** `_show_chapter_story`(지역 입장 region>=1 시 제N장 문구, 4.5초/탭). ④**동료 카피바라** `scenes/Companion.gd`(따라오기 lerp·2.5D 바운스·4초마다 HP+9 힐+초록 파티클·상황별 말풍선[첫만남/위기/보스/독/버프/랜덤]). ⑤**mino 시그니처 워터마크**(우하단 은은하게, layer 3). ⑥**이스터에그** `_update_easter_egg`(좌상단 구석 +10레벨 1회·좌하단 구석 공격 12연타 +12레벨 1회, 흰섬광+파티클+센터메시지). ⑦**소리 토글 버튼**(우상단, 스피커 아이콘/X). ⑧일시정지 중 인트로/스토리 페이드를 위해 ALWAYS 틱 노드. (아군 슬라임은 S3에서 이미 완료라 그대로.) GameState 에 muted·seen_intro 추가(저장/복원 포함). **검증: import 0에러 · 런타임 180프레임 0에러 · S6 통합 31건 ALL PASS(효과음12+BGM 합성/루프·음소거 저장/무탈·인트로 자동표시·페이드·종료·카피바라 따라옴41px/힐10→19/말풍선·이스터에그 좌상단+10/1회제한·좌하단12연타+12·챕터스토리 표시/종료·보스 등장roar/메테오boom 120프레임 무탈·사운드버튼·시그니처) · kill+levelup 사운드 경로 직접 호출 통과(kills 0→1, lvl 1→14) 크래시 0.** 밸런스 변경 없음(연출·사운드만 추가). — *이유: 설계도 S6 마지막 슬라이스. mino1 의 "무음·정지" 약점을 Godot AudioStreamWAV 합성으로 메우고(이번 포팅의 핵심 업그레이드), 인트로·동료·이스터에그로 '진짜 게임' 마감. 포팅 S0~S6 전부 완료.*
- 2026-06-20: **★S5 — 지역·보스·난이도 완료.** `systems/Terrain.gd`(지역별 바위·독웅덩이·버프 크리스탈 + 효과: 바위 밀어냄·독 초당6피해+초록 비네팅·버프 공격+30%·이속+25%) + `scenes/Boss.tscn/gd`(4종 보스 — 멧돼지왕·악어·늑대왕·코끼리, 등장연출[흔들림+섬광]·찌그러짐·돌진예고선·바위충돌 기절·약점 추가타·HP잔상바·메테오·독장판) + `ui/DifficultyPanel.gd`(10단계 2열 그리드, 첫 실행에 자동) + `ui/WorldMapPanel.gd`(4지역 카드·해금[✅/▶/🔒]·입장). Main 에 지형 효과·보스 등장 트리거(킬 12+챕터×4)·보스 피격·지역 입장(enter_region)·챕터 클리어→해금→지도·hazard_damage(난이도 배율+비율방어)·이동배율·처음부터(↻) 버튼 연동. GameState 에 max_region·chapter 추가. **검증: import 0에러 · 단위+통합 38건 ALL PASS(지역별 바위/독/버프 개수·이동배율 보존·결정론 배치·난이도 배율 보존·보스4종 HP[440/650/750/1172] 보존·입장·보스 등장/처치/해금·챕터 클리어 탭·hazard 임파서블 raw10→40·버프 이동배율 0.9) · 보스 4종 각 240프레임 패턴(돌진·메테오·독장판·기절) 크래시 0.** 밸런스(보스 HP·돌진 속도·피해·메테오 개수·독장판·지형 개수·난이도 배율) mino1 그대로. — *이유: 설계도 S5. 월드맵 해금형 RPG 루프와 보스전·난이도 선택을 깔아 S6(연출·사운드 마감)만 남김.*
- 2026-06-20: **★S4 — 장비·요리·골드 완료.** `systems/Inventory.gd`(장착/해제/줍기/판매 단일 권한 — S3 약속대로 무기 스탯 합산을 JobPanel 에서 이관) + `systems/Loot.gd`(바닥 장비·날고기·그릴·요리, 월드 좌표) + `ui/InventoryPanel.gd`(가방 버튼·4×4 장비 창·판매) + `ui/CookHUD.gd`(좌하단 고기 패널·먹기 버튼). Main 에 드랍(`try_drop`·날고기 확률)·콜백·먹기 입력 연동. 밸런스(드랍 12%·판매가·45/3초·회복량) mino1 그대로. **검증: import 0에러 · 단위 33건 ALL PASS · 실 씬 라이브(드랍→자동장착·요리→먹기·패널 판매) 0크래시.** — *이유: 설계도 S4. 장비·골드 순환과 생존 루프(고기)를 깔아 S5(보스 드랍·난이도)가 이 위에 얹힌다.*

## 포팅 진행 (슬라이스)
- [x] **S0 기반 뼈대** — GameData/GameState/Main(맵·카메라)/Player(이동). ✅ 2026-06-20
- [x] **S1 전투 코어 + 손맛** — 적 스폰·추격 AI·근접 공격·데미지·사망·경험치 + 넉백·히트스톱·파티클·임팩트·카메라 흔들림·스윙/찌그러짐. ✅ 2026-06-20 (효과음은 S6)
- [x] **S2 성장** — 레벨업 3택 스킬 선택창(큐) + 스탯 7종 분배+골드환전 + HUD(HP/MP/XP·레벨·직업·킬·골드·퀘스트) + HP/MP 자동회복. ✅ 2026-06-20
- [x] **S3 전직·스킬** — 1차(검사·마법사·도적, Lv10 3택)·2차(마검사·대마법사·닌자, Lv25 1택) 전직 패널 + 기본 스킬 6종 + 클래스 스킬 12종 + 스킬 트레이(쿨·마나) + 전직 스킬 메뉴 + 투사체/메테오/장판/소환/무적/화면플래시. ✅ 2026-06-20
- [x] **S4 장비·요리·골드** — 인벤토리(장착/해제/줍기/판매 단일 권한) + 바닥 장비 드랍·자동줍기 + 4×4 장비 창(가방 버튼) + 희귀도별 판매 + 날고기 드랍·그릴 자동굽기·구운고기 먹기(위기 회복) + 좌하단 요리 HUD. ✅ 2026-06-20 (효과음·고기 버프 효과는 S6/S5)
- [x] **S5 지역·보스·난이도** — 지역별 지형(바위·독·버프) + 4지역 세계지도 해금 + 보스 4종(돌진·메테오·독장판·바위충돌 기절·약점) + 난이도 선택 10단계 + 챕터 클리어→해금 + hazard 피해(난이도 배율) + 처음부터 버튼. ✅ 2026-06-20 (효과음·완전한 게임오버 화면·인트로/챕터 스토리 연출은 S6)
- [x] **S6 연출·사운드 마감** — 사운드 엔진(효과음12종+BGM 합성, 음소거 토글) + 전 호출부 연결 + 인트로 스토리 + 챕터 스토리 + 동료 카피바라(따라오기·힐·말풍선) + mino 시그니처 워터마크 + 이스터에그(좌상단+10/좌하단12연타+12) + 소리 토글 버튼. ✅ 2026-06-20 **(포팅 S0~S6 전부 완료)**

## S6 산출물 위치 (다음 세션 참고)
- **사운드 엔진(오토로드 싱글톤)**: `autoload/Audio.gd` (`Audio`, project.godot autoload 에 GameState 다음 등록). mino1 `sound.js`/`MinoSound.*` 1:1 대응. **핵심: 음원 파일 0개 — `_tone_into`/`_noise_into`/`_tone_pad` 로 PCM 파형을 합성해 `_bake()` 가 16비트 모노 `AudioStreamWAV` 로 굽는다.** 공개 API: `attack/hit/crit/kill/levelup/heal/hurt/roar/boom/tame/win/ui_tap()` + `start_bgm/stop_bgm()` + `set_muted/toggle/is_muted()`. 효과음 풀 8보이스(라운드로빈), BGM 1개(loop_forward, -12db). 합성 결과는 `_cache` 에 보관(한 번만 합성). SR=22050. **새 효과음 추가 = `_synth()` match 에 한 줄.** *외부 CC0 다운로드는 시도했으나 opengameart/github 직링크가 불안정(HTML 404 반환) → 프롬프트 명시 폴백대로 합성 채택. mino1 도 합성이었으니 사운드 톤은 mino1 과 동일.*
- **동료 카피바라**: `scenes/Companion.gd` (`Companion`, Node2D). mino1 `_capybara`/`_updateCapybara`/`_drawCapySpeech`/`_showHealEffect` 대응. Main `_build_companion()` 이 생성. `assets/sprites/capybara.png`. 따라오기(lerp 거리55)·2.5D 바운스·flip·발밑 그림자(코드 생성 타원)·4초마다 dist<120 이면 HP+9 힐+초록 파티클+`Audio.heal()`·말풍선(`speech_*`, 상황별 대사 6종, 쿨다운 10~16초). 말풍선 배경은 child Node2D 가 `_draw`(흰 둥근사각+꼬리), 글자는 Label. main.terrain.poison_active/buff_active·main.boss·main.boss_spawned 를 읽어 대사 결정.
- **Main S6 연동(`scenes/Main.gd` 하단 'S6: 연출·사운드 마감' 블록)**: `_build_companion`·`_build_mino_sig`(layer3 우하단 'mino' 라벨 α0.45)·`_build_sound_button`(layer4 우상단 y158, 스피커/X 아이콘 토글)·`_build_overlay_ticker`(ALWAYS Node — 일시정지 중에도 인트로/스토리 페이드를 굴림). 인트로: `_show_intro`/`_update_intro`/`_end_intro`(intro_layer 20, 오버레이+4줄 페이드인+시그니처+탭종료, 끝나면 difficulty<0 시 diff_panel.open). 챕터스토리: `_show_chapter_story(chapter)`/`_update_chapter_story`/`_end_chapter_story`(story_layer 20). 이스터에그: `_update_easter_egg(dt)`(_process 에서 호출)·`_grant_bonus_levels(n,title)`(섬광+파티클+센터메시지+Audio.levelup). 사운드 호출 삽입 지점은 위 작업로그 ①에 전부 명시. `enter_region` 끝에서 region>=1 이면 `_show_chapter_story(region+1)` 호출(region 0 은 기존 입장배너).
- **GameState 추가**: `muted`(bool, Audio 가 읽고 **save/load 포함**)·`seen_intro`(bool, **세션 한정 — 저장 안 함**, 매 부팅 false 리셋이라 새 세션마다 인트로 한 번). `_ready` 에서 `if GameState.chapter < 1: chapter = region+1` 유지.
- **검증 아티팩트**: `tools/s6_test.gd` (`Godot --headless --path . --script tools/s6_test.gd` 로 S6 31건 재실행 가능 — 오토로드는 root.get_node 로 접근).
- **검증**: import/parse 0에러 · 런타임 180프레임 0에러 · S6 통합 31건 ALL PASS · kill+levelup 경로 직접호출 통과 크래시 0.
- **S6 마무리/주의**: ①소리는 헤드리스 더미 오디오라 화면(WSL)에선 안 들림 — **실기기/브라우저(deploy 후)에서만 실제로 들린다**(배포는 메인이 함). ②웹 export 후 브라우저 자동재생 정책상 BGM/SFX 는 첫 사용자 입력(탭) 후부터 남(인트로 탭이 그 트리거 역할). ③`assets/sfx`·`assets/bgm` 폴더는 합성이라 비어서 삭제함(파일 없음). ④고기 먹기 짧은 버프 *효과*(`apply_food_buff` 의 실제 능력치 적용)는 여전히 자리만 — S6 범위 밖(타이머만 돈다). ⑤완전한 게임오버 화면은 여전히 '같은 자리 부활'(설계도상 S6 마감 대상이었으나, mino1 도 부활형이고 이번 범위는 인트로·스토리·동료·사운드 중심으로 좁혀 구현 — 필요시 후속).

## S5 산출물 위치 (다음 세션 참고)
- **지형 시스템**: `systems/Terrain.gd` (`TerrainSystem`, 월드 Node2D, z=-70). mino1 `_initTerrainZones`/`_updateTerrainEffects` 대응. `init_zones()`(지역 전환 시 재생성 — 지역별 시드 3141+region*777 로 결정론 배치)·`update(dt)`(Main 이 매 프레임 호출). 노출 변수(보스·Main·Player 가 읽음): `obstacles`[{x,y,r}]·`region_move_mult`·`region_elite_rate`·`buff_active`·`poison_active`. 효과: 바위 밀어냄(주인공+적, 타원 1.4 보정)·독 초당6 hazard 피해+화면 가장자리 초록 비네팅(CanvasLayer 6)·버프존 안에서 공격+30%[Main 전투]·이속+25%[player_move_mult] (벗어나도 3초 유지). 밸런스(개수·반경·피해·이동배율·버프배수) mino1 그대로.
- **보스**: `scenes/Boss.gd`+`Boss.tscn` (`Boss`, CharacterBody2D). mino1 `_spawnBoss`계열/`_updateBoss`/`_killBoss`/`_drawBossHP`/`_spawnMeteors` 대응. `setup(type, chapter, player, main)`. 4종 패턴: croc(빠른 돌진380+독장판3개 부채꼴, 4초)·boar_king/wolf_king(돌진→피하면 빈틈 0.9초 / 바위에 박으면 기절 1.7초=약점 2배, 늑대가 더 빠름 450/95)·elephant(느린추격48+메테오 5~6발 궁극기+근접18). 인트로 1.8초(흔들림+붉은 섬광). HP 잔상바=화면고정 CanvasLayer(layer 9), 패턴 그리기(기절별·독장판·메테오 예고원)=월드 좌표 child Node2D(Main 자식, z=3100). **약점(vulnerable/vuln_mult)은 Main._update_combat 보스 피격이 읽어 추가타.** 보스 HP·돌진속도·피해·메테오개수 mino1 보존. 표시높이는 mino1 fitH 값을 minho2 비율로 축소(BOSS_H).
- **난이도 선택**: `ui/DifficultyPanel.gd` (`DifficultyPanel`, layer 13, ALWAYS·일시정지). mino1 `_showDifficultySelect`/`_pickDifficulty` 대응. 10단계 2열 그리드, 첫 실행(difficulty<0)에 `_ready`가 자동 `open()`. 고르면 `GameState.difficulty` 저장→`on_difficulty_picked`→세계 지도.
- **세계 지도**: `ui/WorldMapPanel.gd` (`WorldMapPanel`, layer 12, ALWAYS·일시정지). mino1 `_showWorldMap`/`_handleWorldMapTap`/`_enterRegion` 대응. 4지역 카드 세로 배치+연결선, 상태(✅클리어 i<max_region / ▶입장가능 i<=max_region / 🔒잠김). 열린 카드 탭→`Main.enter_region(idx)`.
- **Main 연동**: `_build_terrain`(fx 뒤·loot 앞)·`_build_region_ui`(난이도 layer13 + 지도 layer12 + 처음부터 버튼 layer4)·`_build_restart_button`(우상단 ↻, STAT 아래 y=108) 추가. `_process` 에 `terrain.update`·보스 등장 트리거(킬>=12+chapter*4). `_update_combat` 에 버프 공격배율·전직 치명타 보너스·**보스 피격 블록**(사거리+80, 약점배수, 생명흡수). 새 메서드: `hazard_damage(raw,inv,color,prefix)`(보스/메테오/독 고정피해 — 난이도 dmg_mult 곱+비율방어 armor/(70+armor), 무적 차단, 게임오버)·`player_move_mult`(지역×버프)·`_spawn_boss`·`on_boss_died`(전설드랍 tryDrop is_boss·골드 80+0~39·잡몹 정리)·`enter_region`(지형 재생성·적/보스 리셋·중앙 이동·저장·입장배너)·`_show_chapter_clear`(다음 지역 해금 max_region·탭→지도)·`_show_region_enter_banner`·`_retint_ground`. `GameState` 에 `max_region`·`chapter` 추가(저장/복원 포함).
- **검증**: import 0에러 · 단위+통합 38건 ALL PASS · 보스 4종 각 240프레임 패턴 크래시 0.
- **S5 임시/다음단계(S6)**: ①효과음(보스 포효·메테오·기절·버프)은 S6. ②완전한 게임오버 화면(현재 같은 자리 부활)·인트로 스토리·챕터 전환 스토리는 S6. ③고기 먹기 버프 효과 연동(`apply_food_buff` 자리만)도 S6. ④Fx 에 `boss_line` 텔레그래프 종류 추가함(보스 돌진 예고선).

## S4 산출물 위치 (다음 세션 참고)
- **인벤토리 시스템(단일 권한)**: `systems/Inventory.gd` (`InventorySystem`, Node). mino1 `_equipItem`/`_unequipItem`/줍기/판매 대응. 메서드: `equip(slot,id)`(이전 것 자동 해제 후 장착, 빠진 이전 id 반환)·`unequip(slot)`·`add_to_bag(id)`·`bag_full()`·`pickup(id)`(빈 슬롯이면 장착·아니면 가방·둘 다 안 되면 false)·`sell(idx)`(희귀도별 골드 반환)·`sell_price(id)`. `INV_MAX=16`. **★S3 약속대로 무기 스탯 합산을 JobPanel 에서 *여기로 이관*** — JobPanel._apply 가 이제 `main.inventory.equip("weapon", ...)` 호출(옛 `_equip_weapon` 은 폴백으로 남음). 데이터: `this._inventory` = `GameState.inventory`(배열 `[{item_id}]`), `p.equip[slot]` = `GameState.player.equip`.
- **바닥 장비·날고기·그릴·요리(월드 좌표)**: `systems/Loot.gd` (`LootSystem`, Node2D, z=50). mino1 `_addGroundItem`/`_updateGroundItems` + `_spawnRawMeat`/`_drawGrill`/`_updateCookingSystem`/`_eatCookedMeat` 대응. `add_ground_item(item)`·`add_raw_meat(x,y)`·`eat_cooked_meat()`. 바닥 장비=희귀도 테두리 둥근 사각(20초 소멸·40px 자동줍기→`inventory.pickup`), 날고기=빨강 타원(45초 상함·44px 자동줍기→`raw_meat`), 그릴 3개(맵 중앙 주변 고정, 68px 근처+날고기 보유→3초 자동굽기→`cooked_meat`, 연기·진행 원호·익는 색 연출). 보유 고기(`raw_meat`/`cooked_meat`)는 mino1 처럼 *저장 안 함*(세션 한정). 먹기=위기(hp<40%) 45·여유 30 회복+mp20. **밸런스(20/45/3초·확률·회복량) mino1 그대로.**
- **장비 창 + 가방 버튼**: `ui/InventoryPanel.gd` (`InventoryPanel`, layer 8, ALWAYS·열리면 일시정지). mino1 `_drawInvBtn`/`_drawInventoryPanel`/`_handleInvTap` 대응. 우상단 가방 버튼(STAT 버튼 위 y=4, 가득 차면 빨강 점)→ 장착 슬롯 3개(탭=해제→가방)+4×4 그리드(첫 탭=선택·판매버튼 표시, 다시 탭=장착)+스탯 요약+용량+처음부터+판매(+골드). 모든 장착/해제/판매는 `main.inventory` 경유.
- **요리 HUD**: `ui/CookHUD.gd` (`CookHUD`, layer 4, 그리기 전용). mino1 `_drawCookingHUD`/`_eatBtnRect`/`_showMeatAlert` 대응. 좌하단 고기 패널(날/익힘 개수)+먹기 버튼(구운고기 있으면 주황 발광)+굽기 진행 메시지(화면 하단)+상함 알림. **입력은 Main `_handle_touch` 가 `try_eat(pos)` 로 조이스틱보다 먼저 검사**(스킬 트레이와 같은 방식).
- **Main 연동**: `_build_inventory`(가장 먼저 — loot·패널·전직이 참조)·`_build_loot`·`_build_inv_ui`(가방창 layer8 + 요리HUD layer4) 추가. `on_enemy_died` 에 장비 드랍(`try_drop`, 엘리트 추가 70%)·날고기 드랍(일반 35%·엘리트 60%) 추가. 콜백: `show_meat_alert`·`spawn_heal_text`(월드 회복 라벨)·`apply_food_buff`(버프 타이머만, 효과는 S5/S6). `_handle_touch` 에 `cook_hud.try_eat` 추가. `food_buff_t` 타이머 `_process` 에서 감소.
- **검증**: import 0에러 · S4 단위테스트 33건 ALL PASS(장착 +스탯/교체 차감/해제 원복·maxhp hp클램프·줍기 빈슬롯→장착/참→가방·가방16가득·판매 희귀도별 골드[고급40·전설340]·그릴3개·굽기 3초→구운고기·먹기 위기45/여유30) · 실 Main 씬 라이브 드라이버(적 강제 처치→장비 바닥드랍→자동장착 `rusty_knife`·날고기 드랍/줍기/굽기/먹기 hp10→55·인벤 패널 열기/선택/판매·골드 누적) 0크래시 · 300프레임 정상부팅 0에러.
- **S4 임시/다음단계**: ①고기 먹기 *짧은 버프*(공격력·이속) 효과는 `apply_food_buff` 자리만 — 버프 시스템 오는 S5/S6 에 연동. ②줍기/판매/굽기 *효과음* 은 S6. ③보스 드랍(`tryDrop is_boss=true`)은 보스 오는 S5 에서 `loot.add_ground_item` 으로 연결.

## S3 산출물 위치 (다음 세션 참고)
- **스킬 엔진**: `systems/Skills.gd` (`SkillSystem`, 월드 Node2D). mino1 `_skills`·`_castSkill`·`_castClassSkill`·`_updateSkills`·`_classSkills`·투사체/메테오/워터폴/소환슬라임/플래시 대응. `update(dt)`(Main 이 매 프레임 호출)·`cast_skill(key)`·`cast_class_skill(def)`·`can_cast`·`my_class_skills`·`skill_dmg_mult`. 투사체(파이어볼/표창)·메테오·워터폴 장판은 자체 `_draw` 로 월드에 그림. **밸런스(마나·쿨·데미지 배수) mino1 그대로** — 기본스킬 cd/mp: fireball 2.0/20, meteor 6.0/35, teleport 4.0/15, invincible 10.0/0(현재마나 전소모), waterfall 5.0/25, summon_ally 12.0/30. 클래스 스킬 12종 cd/mp·배수도 mino1 보존. 마법사 마나회복 +10/초, 대마법사 +18/초·쿨 1.3배·dmgMult 2.6.
- **전직 패널**: `ui/JobPanel.gd` (`JobPanel`, layer 11, ALWAYS·일시정지). mino1 `_openJobPanel`/`_applyJob`/`_openJob2Panel`/`_applyJob2`/`_advJobFor` 대응. `check_unlock()`(Lv10→1차 3택 / Lv25→2차 1택; 떴으면 true)는 **레벨업 큐가 카드 띄우기 전 먼저 호출**(LevelupPanel.process_queue 안). 직업 스탯·기본무기 지급(자동장착, 인벤토리 전이라 무기 스탯 직접 합산·이전무기 스탯 차감)·`equip.weapon` 기록. 1차 스탯: 검사 +90hp/+9armor/+14atk, 마법사 +90mp/-4atk, 도적 +55sp/공속×0.6/-25hp. 2차: 마검사 +120hp/+24atk/+6armor, 대마법사 +150mp, 닌자 +40sp/공속×0.7/+40hp.
- **스킬 트레이**: `ui/SkillTray.gd` (`SkillTray`, layer 4, 그리기 전용·MOUSE_FILTER_IGNORE). mino1 `_drawSkillBtns`/`_getSkillBtnPositions` 대응. 공격 버튼 위 2줄 6개(아래 fireball/meteor/teleport, 위 invincible/waterfall/summon_ally)+직업 있으면 왼쪽에 전직스킬 버튼. 쿨다운 파이·마나부족 회색·아이콘·쿨숫자. **입력은 Main `_handle_touch` 가 `try_hit(pos)` 로 먼저 검사**(조이스틱/공격보다 우선 — 버튼 위 탭이 이동으로 새지 않게). 줄 위치는 공격버튼 사각형 *위쪽 바깥*이라 안 겹침.
- **전직 스킬 메뉴**: `ui/SkillMenu.gd` (`SkillMenu`, layer 9, ALWAYS·일시정지). mino1 `_openSkillMenu`/`_closeSkillMenu` 대응. 트레이의 전직스킬 버튼 → 열림. 준비(쿨0·마나충분) 카드 탭 → 발동 후 닫힘, 카드 밖 → 닫힘.
- **Main 연동**: `_build_skills`(Skills Node2D)·`_build_skill_ui`(SkillTray/SkillMenu)·`_build_flash`(화면 플래시 오버레이 layer 7) 추가, `_build_growth_ui` 에 JobPanel(layer 11) 추가. `_process` 에 `skills.update(dt)`. 콜백 추가: `check_job_unlock`·`show_skill_feedback`·`show_center_message`(전직완료 큰글씨, ALWAYS 트윈)·`show_pickup_toast`·`_load_ally_tex`(slime 재사용). 무적 스킬은 `p.inv` 를 매 프레임 유지해 Player.is_invincible 과 연동.
- **레벨업 큐 흐름 변경**: `ui/LevelupPanel.gd` `process_queue()` 가 카드 띄우기 전 `main.check_job_unlock()` 우선 호출 — 전직(Lv10/25)이 레벨업 스킬 선택보다 먼저(mino1 `_checkJobUnlocks` 순서). 전직 적용 후 JobPanel `_close_and_continue` 가 `process_queue` 를 다시 불러 남은 큐 이어감.
- **검증**: import 0에러 · S3 통합테스트(1차전직 검사 스탯·무기, 2차 마검사 스탯·무기교체 차감, 클래스스킬 4종, 기본6+무적 캐스팅·쿨·마나부족 거부) ALL PASS · 스트레스(마법사→대마법사 dmgMult 2.6, 적 8마리, 620프레임 지속 캐스팅·투사체 충돌) 크래시 0(킬 83) · 정상 부팅 120프레임 0에러.
- **S3 임시/다음단계**: ①무기 스탯 합산을 JobPanel 이 직접 함 — S4 인벤토리 시스템 오면 그쪽으로 이관. ②전직 완료 사운드는 S6. ③mino 시그니처(전직창 하단)는 S6 연출 마감 때.

## S2 산출물 위치 (다음 세션 참고)
- **HUD**: `ui/HUD.gd` (Control + `_draw`). 좌상단 레벨 뱃지·HP/MP/XP 바·직업·킬·골드, 우상단 퀘스트(목표) 텍스트. mino1 `_drawHUD` 대응. 매 프레임 직접 그림(둥근 사각은 `_round_rect` 헬퍼). 숫자는 Label(Jua 폰트)로 위에 얹음. 임시 `info_label` 은 화면 하단 안내/게임오버용으로 강등.
- **레벨업 3택**: `ui/LevelupPanel.gd` (Control). mino1 `_openLvlupPanel`/`_processLevelupQueue`/`_applySkill` 대응. **큐 방식** — 한 번에 여러 레벨이 올라도 `queue_levelup(n)` → `process_queue()` 로 *하나씩* 띄움. 떠 있는 동안 `get_tree().paused=true`(패널은 `PROCESS_MODE_ALWAYS`). 스킬 3개는 **결정론 RNG**(`Main.core_rng()`, 시드=`GameState.seed_val`)로 뽑음. 카드 선택 → `Main.apply_skill(id)`.
- **스탯 분배**: `ui/StatPanel.gd` (Control). mino1 `_drawStatBtn`/`_drawStatPanel`/`_handleStatTap` 대응. 우상단 'STAT' 토글 버튼(포인트 있으면 주황 빛+빨강 뱃지) → 7종 행(공격력·공격속도·방어·최대HP·최대MP·이동속도·경험치획득) + 골드 100→포인트 1 환전. 증가값·클램프(공속 0.12 하한, sp 380, swift 320)·`spentStats` 누적 전부 mino1 보존. 스탯창은 게임을 *멈추지 않음*(mino1 동일).
- **스킬 효과**: `Main.apply_skill()` — sword_mastery(+5 atk)·quick_strike(쿨×0.8)·iron_wall(+3 armor)·swift_wind(+22 sp,≤320)·wide_slash(+14 range)·vitality(+20 maxhp 즉회복). critical·life_drain 은 패시브 → `Main._update_combat` 명중 루프에서 매번 읽음(life_drain +1.5×스택 회복, 치명타+quick_strike 시 다음 쿨 ×0.7).
- **자동회복**: `Main._update_regen` (S1 부터 있음, S2 에서 그대로) — 1초마다 난이도 regen 만큼 HP, MP +2.
- **레이어 순서**(CanvasLayer): HUD=5, info=6, StatPanel=8, LevelupPanel=10(ALWAYS). 조이스틱/공격버튼은 기존 레이어.
- **검증**: import 0에러 · 성장 통합테스트(레벨업 큐 5렙·스킬 4종 큐 소진·일시정지 복귀·7종 분배·골드 환전·전 스탯 분기·regen 회복) ALL OK · 실전 전투 1200프레임(자연 레벨업+패널 자동선택+스탯창 동시) 크래시 0.

## S1 산출물 위치 (다음 세션 참고)
- **적**: `scenes/Enemy.gd` + `Enemy.tscn` (CharacterBody2D). type별 AI(slime/rat/pig/wolf=추격, hedgehog=추격+가시는 S2, boar=돌진 예고, toad=접근(독침 발사는 S3), crow=치고빠지기, croc=매복) + 의사 애니(squash/flap/wobble) + 그림자 lift + 피격 찌그러짐(Tween) + 사망 펑. mino1 _spawnEnemy/_updateEnemies/_applyEnemyAnim 대응. 밸런스 숫자 보존.
- **이펙트**: `scenes/Fx.gd` (Node2D, 월드좌표 _draw). 파티클·임팩트원·예고선/원·슬래시호. mino1 _partGfx/_impactGfx/_telegraphGfx/_slashGfx 대응.
- **전투 루프·스폰·보상**: `scenes/Main.gd` 에 추가 — `_update_spawn`(SPAWN_INTERVAL 1.4·MAX_ENEMIES 10), `_spawn_enemy`(챕터·레벨·난이도·엘리트 배율 mino1 그대로), `_update_combat`(공격→명중→넉백·히트스톱 0.04·카메라흔들림), `_gain_xp`(레벨업 스탯 누적), `on_enemy_died`(파티클·골드·XP), `_update_regen`, 데미지숫자(`spawn_float_text`), 카메라 흔들림(`add_shake`).
- **플레이어 전투**: `scenes/Player.gd` 에 추가 — `start_attack`(스윙 0.26·룽지 0.08·슬래시 0.22), `hurt`(비율 방어 _incomingDmg), `is_invincible`, 무적 깜빡임.
- **공격 버튼**: 화면 오른쪽 아래 원형 터치 버튼(+스페이스키). 조이스틱은 왼쪽 60%라 충돌 없음.
- **히트스톱 구현**: `Main.time_scale`(1→0.08) 을 Player·Enemy·Fx 가 매 프레임 읽어 delta 에 곱함.
- **S1 임시 처리(다음 단계에서 교체)**: ①레벨업 3택 스킬 *선택창* 없음(XP·스탯포인트만 적립 → S2) ②toad 독침 *투사체* 없음(접근·접촉만 → S3) ③hedgehog 가시 반사 없음(S2 스킬) ④게임오버 = 그 자리 풀회복+2초 무적(완전한 게임오버 화면은 S5/S6) ⑤난이도 미선택이면 '보통'(1) 폴백(선택 화면 S5).
- **검증**: import 0에러 · 런타임(600·1200프레임) 0에러 · 강제 전투 통합테스트 1500프레임 → 적 스폰/피격/사망/넉백/XP(lvl1→4)/골드(0→47)/킬(0→11)/HP성장 정상, 크래시 0.

## S0 산출물 위치 (다음 세션 참고)
- 데이터 정본: `autoload/GameData.gd` (core.js 대응). 상태: `autoload/GameState.gd` (this.gs 대응).
- 게임 루프: `scenes/Main.gd`(월드·카메라·조이스틱). 플레이어: `scenes/Player.gd`.
- 스프라이트: `assets/sprites/*.png` (이름 = ENEMY_DEFS sprite 키, 주인공=hero.png, 보스=boss_elephant.png, 동료=capybara.png).
- 옛 시범게임: `prototypes/star_catcher.*` (참고용 보관).
